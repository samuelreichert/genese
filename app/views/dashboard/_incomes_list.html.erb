<div class="row">
  <div class="col-lg-12">
    <p class="lead">Receitas a receber</p>
  </div>

  <% if @entries.empty? %>
    <%= render 'no_data' %>
  <% else %>
    <div class="col-lg-12">
      <table class="table table-hover">
        <tbody>
          <% @incomes.each do |income| %>
            <tr>
              <td>
                <%= income.description %>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  <% end %>
</div>
